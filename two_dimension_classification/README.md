BP算法即反向传播算法，从Loss-y_propa-a2-z2-a1-z1逐步求偏导并通过链式法则来获得所有可调整参数的梯度，然后对他们以特定学习率进行梯度下降

逻辑回归 在只对输入参数线性变换的基础上进行训练，用sigmoid获取Loss

神经网络 在既有线性变换又有激活函数的非线性变换的基础上进行训练

反思是让人望而却步的事情，我们会在所有故事的主人公身上看到反思的力量，但在恶龙被斩首之后，你会陶醉在胜利的喜悦中，还是回顾来时路？“万事皆定”的完备感固然吸引人，但是打扫战场亦是关乎完备感的喜悦能否长久，能否转化为下一次完备感。
？？？？？？
写这个感觉比写代码难

从这个项目中我们获得了哪些反思？
Q:为什么神经网络初始参数不能全为0？
A：

Q：BCE的log中手动添加1e-8来避免log(0),log(1)是明智之举吗?
A: 代码默认用的 float32 大约只有 7 位十进制有效数字，太靠近1会被四舍五入为1，表面看合理，但有两个关键代价：
BCE和手写梯度不完全一致：dz = (p-y)/N 对应标准交叉熵，而我计算的是经过修改的损失。
极端错误的惩罚被封顶：当标签为 1、预测概率趋近 0，损失最多约为 $-\ln 10^{-8}\approx18.42$，而标准交叉熵应该继续增大。
改进方式是$$\ell(z,y)=\max(z,0)-yz+\ln(1+e^{-|z|})$$
改进原理：$$\ell=-y\ln p-(1-y)\ln(1-p)$$带入p=sigmoid(z)得到$$\ln(1+e^z)=z+\ln(1+e^{-z})$$，对z分类讨论后可以得到$$\boxed{\ell=\max(z,0)-yz+\ln(1+e^{-|z|})}$$

